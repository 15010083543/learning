package com.util;

import java.time.*;

public class DateUtils {

    /*public static void main(String[] args) {
        LocalDate localDate = LocalDate.now(); // 获取当前日期
        // Convert LocalDate to long (milliseconds since epoch)
        long millis = localDate.atStartOfDay(ZoneId.systemDefault()).toInstant().toEpochMilli();
        long l = localDate.atTime(LocalTime.MAX).toInstant(ZoneOffset.MAX).toEpochMilli();

        LocalDateTime startOfDay = today.atStartOfDay();
        System.out.println("今天开始时间: " + startOfDay);

        // 获取今天的结束时间（23:59:59.999999999）
        LocalDateTime endOfDay = today.atTime(LocalTime.MAX);
        System.out.println("今天结束时间: " + endOfDay);


        long millis = localDateTime.atZone(ZoneId.systemDefault()).toInstant().toEpochMilli();



        System.out.println("LocalDate: " + localDate);
        System.out.println("Long (milliseconds since epoch): " + millis);
    }*/
}
